# Integration Tests

Integration tests will cover Compose health, LocalAI streaming/unload, ComfyUI native API proxy behaviour, Voicebox health, CPU audio, Model Hub blob semantics, artifact authorization, and restart reconciliation.
